# 通义千问 API 配置
# 获取方式：https://dashscope.console.aliyun.com/
# 1. 登录阿里云账号
# 2. 开通 DashScope 服务
# 3. 在 API-KEY 管理页面创建新的 API Key

ALI_QWEN_API_KEY=sk-your-api-key-here

# 可选：指定模型版本
# qwen-plus-2025-07-28: 最新版本，支持1M上下文，阶梯计费（推荐）⭐
# Qwen-Turbo: 极速版，价格便宜，适合简单任务
# Qwen-Max: 最强版，性能最好但价格较高
ALI_QWEN_MODEL=qwen-plus-2025-07-28

# 后端服务配置
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# 前端配置（如果需要）
FRONTEND_URL=http://localhost:3000

# 可选：同义/包含词族配置（JSON，路径相对项目根目录）
SYNONYM_FAMILIES_PATH=synonyms.json

